{
  "name": "@viberacing/edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bounded Cloudflare origin-proof signer for Vibe Racing Community sync.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.14.0 <25"
  },
  "scripts": {
    "lint": "node --check src/worker.mjs && node --check test/worker.test.mjs",
    "test": "node --test test/worker.test.mjs"
  }
}
