{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.viberacing.example/v1/connector-sync.schema.json",
  "title": "ConnectorSyncV1",
  "description": "Bounded self-reported Community usage submitted by one source-bound device. Derived ranking and trust fields are server-owned and intentionally absent.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "sourceId",
    "syncId",
    "observedAt",
    "connectorVersion",
    "codexVersion",
    "dailyEntries"
  ],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "minimum": 1,
      "maximum": 1
    },
    "sourceId": {
      "type": "string",
      "minLength": 26,
      "maxLength": 26,
      "pattern": "^src_[A-Za-z0-9_-]{22}$"
    },
    "syncId": {
      "type": "string",
      "minLength": 26,
      "maxLength": 26,
      "pattern": "^syn_[A-Za-z0-9_-]{22}$"
    },
    "observedAt": {
      "type": "string",
      "minLength": 24,
      "maxLength": 24,
      "pattern": "^20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
      "format": "date-time"
    },
    "connectorVersion": {
      "type": "string",
      "minLength": 5,
      "maxLength": 64,
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
    },
    "codexVersion": {
      "type": "string",
      "minLength": 5,
      "maxLength": 64,
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
    },
    "dailyEntries": {
      "type": "array",
      "minItems": 1,
      "maxItems": 31,
      "x-viberacing-uniqueBy": "codexReportedDate",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["codexReportedDate", "tokens"],
        "properties": {
          "codexReportedDate": {
            "type": "string",
            "minLength": 10,
            "maxLength": 10,
            "pattern": "^20[0-9]{2}-[0-9]{2}-[0-9]{2}$",
            "format": "date"
          },
          "tokens": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        }
      }
    }
  }
}
