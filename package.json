{
  "name": "viberacing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Weekly leaderboard for coding-agent token usage",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.14.0 <25",
    "pnpm": "11.7.x"
  },
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "audit": "corepack pnpm audit --audit-level moderate",
    "build": "corepack pnpm --filter @viberacing/web build",
    "connector:package:check": "node scripts/check-connector-package.mjs",
    "db:migrate": "corepack pnpm --filter @viberacing/web db:migrate",
    "dev": "corepack pnpm --filter @viberacing/web dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "corepack pnpm --filter @viberacing/web lint",
    "migrations:check": "node scripts/check-migrations.mjs && node --test scripts/check-migration-history.test.mjs",
    "local:down": "sh scripts/local-down.sh",
    "local:reset": "sh scripts/local-reset.sh",
    "local:test": "node --env-file=apps/web/.env.local scripts/test-local-scenarios.mjs",
    "local:up": "sh scripts/local-up.sh",
    "privacy:check": "node scripts/check-privacy.mjs",
    "test": "node --test scripts/check-connector-release.test.mjs scripts/check-connector-version-bump.test.mjs scripts/publish-connector-workflow.test.mjs && corepack pnpm --filter @viberacing/web test && corepack pnpm --filter @viberacing/connector test",
    "test:e2e": "corepack pnpm db:migrate && corepack pnpm --filter @viberacing/web test:e2e",
    "typecheck": "corepack pnpm --filter @viberacing/web typecheck",
    "verify": "corepack pnpm run privacy:check && corepack pnpm run migrations:check && corepack pnpm run connector:package:check && corepack pnpm run format:check && corepack pnpm run lint && corepack pnpm run typecheck && corepack pnpm run test && corepack pnpm run build"
  },
  "devDependencies": {
    "@viberacing/connector-0.4.4": "npm:@viberacing/connector@0.4.4",
    "prettier": "3.9.4"
  }
}
